# VoiceMedAI backend — FastAPI pipeline
PyJWT>=2.8.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0
python-dotenv>=1.0.0
pyttsx3>=2.90
numpy>=1.24.0

# Real speech recognition (recommended — ~75MB tiny model on first run)
openai-whisper>=20231117

# HTTP client — used by Groq ASR cloud API call
httpx>=0.27.0

# Triage classification models (models/triage_models.joblib)
scikit-learn>=1.4.0
joblib>=1.3.0

# Optional: decode WebM on server if browser sends non-WAV
av>=12.0.0

# Text-to-speech for Nigerian accent (en-NG)
edge-tts>=7.2.8

# Machine Learning & Triage Model
pandas>=2.0.0
scikit-learn>=1.2.0
joblib>=1.3.0


